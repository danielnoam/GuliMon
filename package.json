{
  "name": "gulimon",
  "private": true,
  "type": "module",
  "description": "GuliMon - static GuliDex site, no backend. Dev-only tooling for tests and CI validation scripts.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "image-size": "^1.1.1"
  }
}
