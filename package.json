{
  "name": "gulimon",
  "private": true,
  "type": "module",
  "description": "GuliMon - GuliDex site with a Cloudflare Pages Function for automated submission. Dev-only tooling for tests, local preview, and CI validation scripts.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
